name: table_management
description: A restaurant table management and billing system
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.1
  http: ^1.1.0
  provider: ^6.1.1
  image_picker: ^1.0.4          # for picking images
  image_picker_web: ^3.0.0+1
  cross_file: ^0.3.3+4
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true